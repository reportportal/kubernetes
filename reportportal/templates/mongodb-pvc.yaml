{{- if and (.Values.persistence.mongodb.enabled) (not .Values.persistence.mongodb.useExisting) }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: reportportal-mongodb
  labels: {{ include "labels" . | indent 4 }}
spec:
  accessModes:
  - {{ .Values.persistence.mongodb.spec.accessMode | quote }}
  resources:
    requests:
      storage: {{ .Values.persistence.mongodb.spec.capacity.storage | quote }}
{{- if .Values.persistence.mongodb.spec.storageClass }}
{{- if (eq "-" .Values.persistence.mongodb.spec.storageClass) }}
  storageClassName: ""
{{- else }}
  storageClassName: "{{ .Values.persistence.mongodb.spec.storageClass }}"
{{- end }}
{{- end }}
{{- end }}
